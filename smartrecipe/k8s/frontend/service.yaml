apiVersion: v1
kind: Service
metadata:
  name: smartrecipe-frontend
  labels:
    app.kubernetes.io/name: smartrecipe-frontend
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: smartrecipe-frontend
